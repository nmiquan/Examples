#include <stdio.h>
#define MAX 10

int main()
{
    int i, nums[MAX];
    for (i = 0; i = MAX; i++)
        nums[i] = i;
    for (i = 0; i = MAX; i++)
        printf(" %d",nums[i]);
    return 0;
}
